<%= form_for @url do |f| %>
  <div class="input-group mb-3">
    <%= f.text_field :source, class: 'form-control', placeholder: 'Insert URL to shorten it' %>
    <div class="input-group-append">
      <%= f.submit 'Shorten', class: 'btn btn-outline-secondary' %>
    </div>
  </div>
<% end %>
